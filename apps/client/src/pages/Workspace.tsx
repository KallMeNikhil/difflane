import { PlaceholderNotice } from "../components/common";

export default function Workspace() {
  return (
    <PlaceholderNotice
      icon="terminal"
      title="Workspace"
      description="The collaborative review workspace — editor, diff viewer, and live presence — arrives in later milestones."
    />
  );
}
